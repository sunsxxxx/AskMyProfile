# RabbitMQ

## 掌握程度

能够完成交换机、队列、路由键、生产确认、消费确认、重试和死信队列的基础配置，理解消息可能重复以及业务侧必须幂等。

## 实际使用

在“智能仓储协同平台”中异步传递库存变更事件。使用 outbox 表降低数据库提交成功但消息未发送的风险；消费者以事件 ID 和数据库唯一约束实现幂等。

## 异常处理

失败消息按 10 秒、1 分钟、10 分钟重试，超过次数进入死信队列并告警。对积压问题会先看生产速率、消费耗时、失败率和下游数据库，而不是直接无限增加消费者。

## 复盘

没有 RabbitMQ 集群扩缩容和跨地域容灾的生产运维经验。
